/*const db = require('../app'); // O donde sea que hayas configurado tu conexión a la base de datos

const Criterios = {};

Criterios.getAll = (callback) => {
  db.query('SELECT * FROM Criterios', callback);
};

module.exports = Criterios; */