{
    "name": "tolerante",
    "version": "1.0.0",
    "description": "Proyecto con frontend React y backend Node.js",
    "scripts": {
        "dev": "concurrently \"npm run dev:cliente\" \"npm run dev:servidor\"",
        "dev:cliente": "cd frontend react && npm run dev",
        "dev:servidor": "cd backend node && npm run dev"
    },
    "devDependencies": {
        "concurrently": "^6.2.2"
    },
    "dependencies": {
        "axios": "^1.6.8",
        "fs": "^0.0.1-security",
        "nodemailer": "^6.9.13",
        "nodemailer-sendgrid-transport": "^0.2.0"
    }
}
